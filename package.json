{
  "name": "auto-research-shorts",
  "version": "1.0.0",
  "description": "Ferramenta de pesquisa de vídeos para o canal Zuefy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types && tsc --noEmit",
    "test": "node --test workflow/tests/*.test.js"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.14.4",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.68.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
