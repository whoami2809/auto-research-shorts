{
  "name": "auto-research-shorts",
  "version": "1.0.0",
  "description": "Ferramenta de pesquisa de vídeos para o canal Zuefy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "postinstall": "pip3 install yt-dlp --break-system-packages 2>/dev/null || pip install yt-dlp 2>/dev/null || echo 'yt-dlp: instalacao manual necessaria'"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.14.4",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.68.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
